import {Router} from 'express';import {OperatorController} from '../controllers/operator.controller.js';import {auth} from '../middleware/auth.middleware.js';import {roles} from '../middleware/role.middleware.js';const r=Router();r.use(auth,roles('CENTRE_OPERATOR','ADMIN'));r.get('/farmer',OperatorController.farmer);r.post('/farmer/verify',OperatorController.verify);r.post('/procurement',OperatorController.procurement);r.get('/procurement/:id',OperatorController.get);r.post('/receipt',OperatorController.receipt);export default r;