import {Router} from 'express';import {OfficerController} from '../controllers/officer.controller.js';import {auth} from '../middleware/auth.middleware.js';import {roles} from '../middleware/role.middleware.js';const r=Router();r.use(auth,roles('PROCUREMENT_OFFICER','ADMIN'));r.get('/tokens',OfficerController.list);r.patch('/tokens/:id/status',OfficerController.status);export default r;