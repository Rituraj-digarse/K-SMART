import {Router} from 'express';import {CentreController} from '../controllers/centre.controller.js';import {auth} from '../middleware/auth.middleware.js';import {roles} from '../middleware/role.middleware.js';const r=Router();r.get('/',auth,CentreController.list);r.get('/recommended',auth,roles('FARMER'),CentreController.rec);r.get('/:id',auth,CentreController.get);export default r;