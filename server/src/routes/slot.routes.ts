import {Router} from 'express';import {SlotController} from '../controllers/slot.controller.js';import {auth} from '../middleware/auth.middleware.js';import {roles} from '../middleware/role.middleware.js';const r=Router();r.get('/availability',auth,SlotController.availability);r.post('/',auth,roles('ADMIN','PROCUREMENT_OFFICER'),SlotController.create);r.get('/my',auth,roles('FARMER'),SlotController.mine);r.get('/:id',auth,SlotController.get);r.patch('/:id/cancel',auth,roles('ADMIN','PROCUREMENT_OFFICER','FARMER'),SlotController.cancel);export default r;