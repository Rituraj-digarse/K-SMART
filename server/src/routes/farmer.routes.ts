import {Router} from 'express';import {FarmerController} from '../controllers/farmer.controller.js';import {auth} from '../middleware/auth.middleware.js';import {roles} from '../middleware/role.middleware.js';const r=Router();r.use(auth,roles('FARMER'));r.post('/register',FarmerController.register);r.get('/me',FarmerController.me);r.put('/me',FarmerController.update);export default r;