import {Router} from 'express';import {PaymentController} from '../controllers/payment.controller.js';import {auth} from '../middleware/auth.middleware.js';import {roles} from '../middleware/role.middleware.js';const r=Router();r.get('/my',auth,roles('FARMER'),PaymentController.my);r.get('/:farmerId',auth,roles('FARMER','ADMIN','PROCUREMENT_OFFICER'),PaymentController.farmer);export default r;